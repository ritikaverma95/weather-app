services:
  - type: web
    name: weather-app
    env: python
    buildCommand: "pip install -r requirements.txt && npm install && npm run build"
    startCommand: "gunicorn app:app"
    plan: free
    envVars:
      - key: WEATHER_API_KEY
        sync: false
